#pragma once

// Archetypes are templated with the components they are defined by.

namespace gxe {

template<typename ...AComponents>
class archetype {
public:

private:
    // Archetypes have a signature
    // Archetypes have a mapping from type -> 

};

} // namespace gxe